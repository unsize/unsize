import styled from 'styled-components';
import { elemSpacingXs, fontSizeNormal, workSansFont } from './constants';

const P = styled.p`
  font-family: ${workSansFont};
  font-size: ${fontSizeNormal};
  margin: ${elemSpacingXs};
`;

export default P;
